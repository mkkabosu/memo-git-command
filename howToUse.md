#gitの使い方的な何か

##command list
```	$git status```

```	$git add "ファイル名"```

```	$git commit -m "コメント"```

``` $git push origin master```

``` $git checkout -b```

``` $git status pop/push```

``` $git clone https://github.com/ユーザー名/リポジトリ名/```	